<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.benjaminpotzmann.odermanager.deliveryclient">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="at.benjaminpotzmann.odermanager.deliveryclient.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="at.benjaminpotzmann.odermanager.deliveryclient.activity.DisplayOrdersActivity"
            android:label="@string/title_activity_display_orders"
            android:parentActivityName="at.benjaminpotzmann.odermanager.deliveryclient.activity.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="MainActivity" />
        </activity>
    </application>
</manifest>
